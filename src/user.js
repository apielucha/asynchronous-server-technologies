module.exports = {

  save: function(user, callback) {
    // TODO: Saves given user in database

    callback(user);
  },
  get: function(id, callback) {
    // TODO: Fetch user with given ID

    callback(id);
  }

};
